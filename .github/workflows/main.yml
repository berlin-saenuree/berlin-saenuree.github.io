name: CD

on:
  push:
    branches:
      - gh-pages

jobs:
  build:
    name: react build & deploy
    runs-on: ubuntu-latest
    steps:
      - name: install npm dependencies
        run: npm install

      - name: react build
        run: npm run build
